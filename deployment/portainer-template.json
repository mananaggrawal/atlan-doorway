{
  "version": "3",
  "templates": [
    {
      "type": 3,
      "title": "Doorway",
      "name": "doorway",
      "description": "Self-hosted knowledge base for AI agents and people: skills, tools and processes in a git-backed workspace, served to any agent over MCP, with change requests gating every edit.",
      "note": "Fill in the four required values below, then deploy. The app is served on host port 3001 (change with APP_PORT) — put your own reverse proxy in front for TLS. Sign in with the admin email and password; the setup screen collects the knowledge-base repository on first sign-in. Generate the two secrets with: <code>node -e \"console.log(require('crypto').randomBytes(32).toString('base64'))\"</code>. Details: https://github.com/mananaggrawal/atlan-doorway",
      "categories": ["knowledge-base", "AI", "tools"],
      "platform": "linux",
      "logo": "https://raw.githubusercontent.com/mananaggrawal/atlan-doorway/main/docs/hero-light.svg",
      "repository": {
        "url": "https://github.com/mananaggrawal/atlan-doorway",
        "stackfile": "deployment/docker-compose.portainer.yml"
      },
      "env": [
        {
          "name": "ADMIN_EMAIL",
          "label": "Admin email",
          "description": "The deployment owner — always an admin, whatever the sign-in method."
        },
        {
          "name": "ADMIN_PASSWORD",
          "label": "Admin password",
          "description": "Bootstrap sign-in password for the admin email."
        },
        {
          "name": "JWT_SECRET",
          "label": "JWT secret",
          "description": "Signs login sessions. Any long random string."
        },
        {
          "name": "SECRETS_ENC_KEY",
          "label": "Secrets encryption key",
          "description": "32-byte key (base64 or hex) encrypting stored secrets and tokens."
        },
        {
          "name": "DOORWAY_VERSION",
          "label": "Version",
          "default": "0.15.1",
          "description": "Release to run (the published image tag). Pin it; bump deliberately to upgrade."
        }
      ]
    }
  ]
}
